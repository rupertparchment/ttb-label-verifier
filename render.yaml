# Render.com blueprint — deploy as a single Docker web service
services:
  - type: web
    name: ttb-label-verifier
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: OPENAI_API_KEY
        sync: false
